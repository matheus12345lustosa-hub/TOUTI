import { CustomerDisplay } from "@/modules/sales/components/CustomerDisplay";

export default function MonitorPage() {
    return (
        <main className="min-h-screen bg-black">
            <CustomerDisplay />
        </main>
    );
}
